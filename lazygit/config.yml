git:
  pagers:
    - colorArg: always
      pager: delta --dark --paging=never --line-numbers --true-color=always --hunk-header-style=omit

customCommands:
  - key: G
    description: Lazycommit
    command: bash ~/.config/lazygit/lazycommit.sh
    context: files
    output: terminal
